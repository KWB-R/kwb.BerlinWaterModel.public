# Check if function and calculate flow

Check if function and calculate flow

## Usage

``` r
check_if_function_and_calculate_flow(df, q)
```

## Arguments

- df:

  data frame with one row containing column
  'section_out_function_parsed' with function for calculating flow

- q:

  flow vector

## Value

if function: calculated flow vector, if not: NA_real
