% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_functions.R
\name{log10_minor_breaks}
\alias{log10_minor_breaks}
\title{Generate minor tick marks for a log10 axis}
\usage{
log10_minor_breaks(minor_base = 1:9)
}
\arguments{
\item{minor_base}{A numeric vector of factors (default is 1:9) to be multiplied by each power of 10.
This defines which intermediate ticks (e.g., 2, 3, ..., 9) are included between each decade (e.g., between 0.1 and 1).}
}
\value{
A function that takes a numeric range and returns a numeric vector of minor tick positions.
}
\description{
Returns a function that computes minor tick positions for a log10-scaled axis.
This is useful in conjunction with \code{scale_y_log10()} or \code{scale_x_log10()} in \code{ggplot2},
where minor breaks are not added by default.
}
\examples{
\dontrun{
ggplot2::scale_y_log10(
  limits = c(0.01, 100),
  breaks = c(0.01, 0.1, 1, 10, 100),
  minor_breaks = kwb.BerlinWaterModel::log10_minor_breaks()
)
}
}
