% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fill_month_to_start.R
\name{fill_month_to_start}
\alias{fill_month_to_start}
\title{Expand monthly end-of-month values to all days or hours of the same month}
\usage{
fill_month_to_start(
  df,
  date_col = "date",
  datetime_col = "datetime",
  temporal_res = c("auto", "days", "hours"),
  date_min = NULL,
  date_max = NULL
)
}
\arguments{
\item{df}{A data frame or tibble.}

\item{date_col}{Name of the daily date column (default: "date").}

\item{datetime_col}{Name of the hourly datetime column
(default: "datetime").}

\item{temporal_res}{One of "auto", "days", "hours". If "auto",
the presence of \code{date_col} / \code{datetime_col} decides.}

\item{date_min}{Optional lower bound of the output time window. For
daily mode this is interpreted as \code{Date} (or converted via
\code{as.Date()}); for hourly mode it can be a \code{POSIXct} or \code{Date}.
If \code{NULL}, no lower cropping is applied.}

\item{date_max}{Optional upper bound of the output time window.
Same interpretation as \code{date_min}. If \code{NULL}, no upper cropping
is applied.}
}
\value{
A tibble where each original row is expanded so that all days (for
"days") or all hours (for "hours") from the 1st of the respective
month up to the original timestamp are present. All other columns
are copied to the created rows. The result is then cropped to
\verb{[date_min, date_max]} if those are supplied. The name of the time
column is preserved ("date" or "datetime").
}
\description{
For each row in \code{df}, this function assumes that the time column
contains a monthly value at (or near) the end of a month
(e.g. "2002-01-31" or "2002-01-31 23:00:00") and expands it backwards
to all days or all hours from the 1st of that month up to the given
timestamp.
}
\details{
If \code{temporal_res = "auto"} (default), the function chooses
daily expansion when a column \code{date} is present and hourly expansion
when a column \code{datetime} is present.

The resulting series can optionally be cropped to a user-defined time
window via \code{date_min} and \code{date_max}.
}
